#include <stdio.h>
#include <stdlib.h>

void main() 
{
	int num, res;
	printf("nhap so can kiem tra");
	scanf("%d", num);
	
	res = num % 2;
	if( res ==0)
	 printf(" so chan");
	else
	 printf("so le");

	 
}
